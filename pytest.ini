[pytest]
testpaths = tests
addopts = -m "not integration and not live"
markers =
    integration: opt-in tests that may access external APIs or data stores
    live: opt-in tests that call a configured live LLM provider
